interface:
  display_name: "Johnny Suede Design"
  short_description: "Design anything Suede ships — brand, product UI, or tokens — with context gate, design laws, and visual QA."
  default_prompt: "Use $johnny-suede-design for [design request]. Classify the register (brand or product), run the context gate, apply the right design laws, and verify through visual QA before handoff."
policy:
  allow_implicit_invocation: true
