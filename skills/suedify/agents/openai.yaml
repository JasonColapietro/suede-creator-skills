interface:
  display_name: "Suedify"
  short_description: "Match a reference style with screenshot QA"
  default_prompt: "Use $suedify with a reference URL and target URL to restyle the target with Suede-safe fidelity, matched screenshots, token distillation, legal/brand boundaries, and visual QA."
policy:
  allow_implicit_invocation: true
