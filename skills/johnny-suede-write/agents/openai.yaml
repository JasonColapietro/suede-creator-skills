interface:
  display_name: "Johnny Suede Write"
  short_description: "Write anything Suede ships — long-form, short-form, GitHub, social, email — with frameworks, variants, and a copy score."
  default_prompt: "Use $johnny-suede-write to write [surface type and subject]. Identify the mode, choose the persuasion framework, generate variants, apply the anti-slop gate, and deliver a scored output."
policy:
  allow_implicit_invocation: true
